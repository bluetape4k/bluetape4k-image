# Issue #549 provider-neutral ImageClassifier API 경계 7-Tier 검토

## 검토 상태

| 항목 | 값 |
|---|---|
| 대상 | #549 Type-E research/spec/plan/lesson 문서 train |
| 기준 base | `develop` @ `979b45a7865b172c250e199d338e9ad8b1c03732` |
| 작업 branch | `docs/issue-549-api-boundary` |
| 변경 범위 | `docs/superpowers/{research,specs,plans,lessons,reviews}` 5개 문서만 허용 |
| production 변경 | Kotlin source, test, dependency, module, model, native runtime 변경 없음 |
| 독립 reviewer lane | 초기 `REQUEST CHANGES` — P0 0건, P1 3건, P2 2건, P3 0건; 수정 후 resolution lane 재검토 `PASS` — P0/P1/P2/P3 모두 0건 |
| inline leader review | P0 0건, P1 0건, P2 0건, P3 0건 — 모든 독립 지적을 수정한 뒤 재검토 PASS |

## 독립 reviewer lifecycle

`issue549_independent_review`를 `code-reviewer` role의 read-only exact-worktree
lane으로 할당했다. 초기 lane은 2026-08-25T13:05:05Z에 새 결과 없이 bounded deadline을
넘어 `suspect_stall`이 되었고, workflow receipt에 stall·probe를 기록했다. probe grace
안에 초기 독립 reviewer가 `REQUEST CHANGES`를 반환했으므로 이를 timeout PASS로
승격하지 않고 다섯 finding을 leader가 수정했다. 이후 parent-linked
`independent-review-issue-549-rereview` resolution lane도 별도 bounded stall/probe
receipt를 남겼고, fresh exact-head 재검토에서 P0/P1/P2/P3 모두 0건 `PASS`를 반환했다.

## 7-Tier inline 결과

| Tier | 검토 범위 | 결과·증거 |
|---|---|---|
| 1. 범위·의존성 | #549 제외 범위, #543 → #548 → #549 → #550 → #551 순서, Type-E 경계 | **PASS** — research/spec/plan/lesson 모두 public API·module·dependency·model·native mutation을 차단하고 #551 ADOPT 이후 Type-A로 이관한다. |
| 2. API·provider 경계 | `ImmutableImage`, identity/capabilities/options/result/failure, sync/suspend, provider artifact 분리 | **PASS (수정 후)** — `ClassifierCapabilities`를 identity에 명시하고 ORT/JNI/NDArray/Path/URL/native handle을 API signature와 API dependency graph에서 배제했다. `bluetape4k-images-classification-api`와 `bluetape4k-images-classification-onnxruntime`을 분리했다. |
| 3. Kotlin·coroutine·native lifecycle | immutable DTO, cancellation, dispatcher, `AutoCloseable` classifier, `SessionOptions`/session/result/RunOptions/pinned output ownership | **PASS (design-only, 수정 후)** — `$bluetape-kotlin-patterns` production check는 N/A이며, `CancellationException` 재전파와 close/use invariant, resource close 순서를 후속 Type-A fixture로 고정했다. 실제 ORT 실행은 이 문서의 증거가 아니다. |
| 4. serialization·호환성 | Jackson 3 기본값, public wire leakage, schema/unknown/duplicate/trailing/limit, Java compatibility | **PASS (수정 후)** — public timeout을 `java.time.Duration`으로 바꾸고 data-class `Serializable`/`serialVersionUID` workspace 규칙과 canonical JSON wire를 분리했다. Jackson 3는 private implementation-only이며 `kotlinx.serialization`, mapper, default typing, class-name polymorphism을 common public API에서 제외했다. |
| 5. 공급망·보안 | #548 manifest identity, SHA-256, labels/preprocess/postprocess, managed root, external-data/custom-op/remote download | **PASS (blocked-by-design)** — fail-closed 정책과 stable identity를 명시했으며 실제 model bytes·license·SBOM·attestation은 #548/#551의 PENDING gate로 남겼다. |
| 6. 검증·CI·재현성 | source ledger, official ORT URLs, baseline, docs validation, native/hosted CI 해석 | **PASS** — source/relative-link path audit 누락 0, Markdown EOF/fence/whitespace PASS, terminology audit findings=0. `:bluetape4k-images:test` 704 tests/18 skipped는 변경 전 baseline이며 native proof가 아니다. docs-only hosted CI는 path scope상 N/A다. |
| 7. writer·release·metadata | Korean SPW-01~05, issue metadata, DoD, PR handoff | **PASS (pre-PR)** — Korean technical register와 token 보존, SPW-01~05를 각 산출물에 기록했다. PR exact head·body·assignee·milestone·labels·checks는 PR 생성 후 재확인한다. |

## Findings와 disposition

| 등급 | finding | disposition |
|---|---|---|
| P0 | 없음 | 해당 없음 |
| P1 | capability 계약이 research/spec에서 불일치 | `ClassifierCapabilities`를 명시하고 identity·options·fake fixture·Java fixture 계획을 일치시켰다. 해결 |
| P1 | Java-friendly API와 `kotlin.time.Duration` 충돌 | public timeout을 `java.time.Duration`으로 변경하고 non-null Java compile fixture를 계획했다. 해결 |
| P1 | native lifecycle ownership/close 순서 부재 | `ImageClassifier : AutoCloseable`, `SessionOptions → OrtSession → RunOptions/result/pinned-output` ownership과 close race fixture를 명시했다. 해결 |
| P2 | Gradle project ID와 실제 directory path 혼동 | 계획 파일 경로를 `images-classification-api/...`, `images-classification-onnxruntime/...`로 정정했다. 해결 |
| P2 | `Serializable` workspace 규칙과 JSON-only 문구 충돌 | marker/`serialVersionUID`는 source/ABI 관례, canonical JSON은 별도 wire라는 구분을 명시했다. 해결 |
| P3 | 없음 | native/model quality/benchmark/adoption 항목은 결함이 아니라 명시된 후속 #550/#551 gate다. |

독립 reviewer의 초기 `REQUEST CHANGES`는 위 다섯 항목으로 한정되며, 수정된 문서에
대해 leader가 exact worktree를 다시 읽고 inline 7-Tier를 수행해 P0/P1/P2/P3 0건으로
확인했다. 이어진 parent-linked 독립 resolution lane도 수정 내용을 재검토해
P0/P1/P2/P3 0건 `PASS`를 반환했으며, 두 결과와 failed-lane resolution은 workflow
receipt에 각각 보존했다.

## 잔여 gate

1. #550에서 CPU native matrix, Java 25 native access, ORT version/license/SBOM,
   BOM·consumer·CI fan-out을 실행한다.
2. #551에서 동일 model/corpus의 quality, latency/RSS/thread/session, provenance와
   `ADOPT`/`DEFER`/`REJECT`를 결정한다.
3. #551 `ADOPT` 전에는 이 review가 Type-A 구현이나 model adoption을 승인하지 않는다.
4. PR 생성 뒤 live exact head, metadata, checks, linked issue와 final `## DoD Status`를
   다시 읽는다.

## 검증 증거

- `git diff --check`: PASS.
- Korean terminology audit: 5개 문서, `findings=[]`.
- Markdown EOF/fence/trailing whitespace audit: 5개 문서, 오류 0.
- repository-relative link/source path audit: 누락 0.
- `./gradlew :bluetape4k-images:test`: 변경 전 baseline `704 tests, 18 skipped`,
  `BUILD SUCCESSFUL`; docs-only 변경의 native/ORT 증거로 해석하지 않음.
- live `gh issue view 549`: OPEN, assignee `debop`, milestone `1.0.0`, labels
  `documentation`/`enhancement`.
- Kotlin 공식 [inline/value class Java interop](https://kotlinlang.org/docs/inline-classes.html)
  source와 ORT lifecycle URLs를 문서 source ledger에 보존했다.
- workflow run `20260825T123147Z-fe2d95d8`: main lane, 초기 독립 lane,
  parent-linked resolution lane을 별도 receipt로 추적하며 `suspect_stall`·probe·
  REQUEST CHANGES·fresh PASS·failed-lane resolution과 leader inline repair를 분리한다.

## 판정

`PASS — 독립 초기 REQUEST CHANGES(P1=3/P2=2) 수정, inline 7-Tier 및 독립 resolution 재검토 PASS / Type-A 구현·native·adoption BLOCKED`

이 판정은 #549 문서 설계 입력의 완성도만 승인한다. ORT가 실제로 동작한다거나
특정 model을 채택한다는 의미가 아니며, merge 승인도 포함하지 않는다.

## Writer DoD

- `SPW-01`: `PASS` — 대상·범위·7-Tier·독립 lane 상태를 고정했다.
- `SPW-02`: `PASS` — Tier별 증거, findings, disposition, 잔여 gate를 기록했다.
- `SPW-03`: `PASS` — 한국어 기술 문체와 API/URL/command/version token을 보존했다.
- `SPW-04`: `PASS` — local source ledger, official ORT source, live issue와 workflow receipt를 대조했다.
- `SPW-05`: `PASS` — 독립 초기 REQUEST CHANGES, leader 수정·inline 재검토,
  parent-linked 독립 resolution PASS를 분리하고, 수정 후에도 native/adoption PENDING을
  PASS로 승격하지 않았다.
