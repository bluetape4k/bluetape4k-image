# Image 0.3 Manual Diagram Review

Reviewed the five manual diagrams after rendering them at 3200 x 2080 pixels.

## Contract checks

- Dark manual palette is used consistently.
- Every connector has a 14 x 14 arrowhead and a visible destination.
- Routes that share a region use contrasting colors or separate entry points.
- SVG titles and descriptions are present for accessibility.
- Every diagram has a matching SVG and PNG asset.

## Full-size visual review

- `overview/repository-learning-map`: labels remain inside cards; the three learning branches and workshop convergence are unambiguous.
- `architecture/processing-lifecycle`: shortened narrow-card labels prevent overflow; main, retry, and coroutine-boundary paths remain visually distinct.
- `backends/backend-decision-map`: JVM, JNI, and FFM branches have separate colors and no hidden connector segment.
- `integrations/ocr-web-flow`: Spring and Ktor enter the image boundary at separate points; OCR, storage, and operations arrows terminate visibly at their cards.
- `benchmarks/benchmark-interpretation-map`: overlapping fan-out routes use contrasting colors; environment and decision arrows remain visible.

## Verification

```text
ruby scripts/manual/render_image_diagrams.rb
ruby scripts/manual/validate_diagrams.rb
Image diagram contract passed: 5 SVG/PNG pairs, accessible labels, directed connectors, and 14x14 arrowheads.
```
