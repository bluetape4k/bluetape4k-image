name: Code Quality Analysis

on:
  schedule:
    - cron: '0 2 * * *'  # 매일 UTC 02:00 (KST 11:00)
  workflow_dispatch:

env:
  JAVA_VERSION: '21'
  JAVA_DISTRIBUTION: 'temurin'
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  codeql:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [java-kotlin, actions]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}

      - name: Setup JDK
        if: matrix.language == 'java-kotlin'
        uses: actions/setup-java@v5.6.0
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}

      - uses: gradle/actions/setup-gradle@v6.2.0
        if: matrix.language == 'java-kotlin'
        with:
          gradle-version: wrapper

      - name: Pin Kotlin for CodeQL extractor
        if: matrix.language == 'java-kotlin'
        run: |
          ./gradlew help --no-daemon
          catalog="$(find .gradle/bluetape4k-dependencies -type f -name libs.versions.toml -print -quit)"
          test -n "$catalog"
          codeql_catalog="$GITHUB_WORKSPACE/.gradle/codeql-libs.versions.toml"
          cp "$catalog" "$codeql_catalog"
          perl -0pi -e 's/^kotlin\s*=\s*"2\.4\.0"/kotlin = "2.3.21"/m' "$codeql_catalog"
          grep -n '^kotlin\s*=\s*"2\.3\.21"' "$codeql_catalog"

      - name: Build with Gradle
        if: matrix.language == 'java-kotlin'
        run: ./gradlew build -x test --no-daemon -Pbluetape4kDependenciesCatalogPath="$GITHUB_WORKSPACE/.gradle/codeql-libs.versions.toml"
        env:
          GRADLE_OPTS: "-Dorg.gradle.daemon=false"

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: '/language:${{ matrix.language }}'
