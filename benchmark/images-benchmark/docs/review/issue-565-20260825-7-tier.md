# Issue #565 Train-3 7-Tier 리뷰

## 리뷰 범위

- 대상: `feat/issue-565-corpus-train3`, 최종 리뷰 HEAD `e4da40c`
- 기준: Issue #565의 corpus protocol runner, cold/warm 의미, 실제 throughput·RSS 측정, manifest·CER/WER receipt 무결성, Kotlin 패턴, 테스트, 문서, CI 범위
- 원래 독립 lane: `independent_review_train3`, exact target `89d75e876359917f0a9bae7a83735e9310c75b31`
- fallback lane: `independent_review_train3_inline`
- 변경 경계: reviewer lane은 read-only(`write_scope=[]`)이며, 구현 수정은 root 실행 lane에서 별도로 반영했습니다.

## Lifecycle 결과

native reviewer는 startup ACK 뒤 bounded probe 기간에 결과를 반환하지 않아 `suspect_stall`로 기록했습니다. probe deadline 경과 후 interrupt를 기록하고, 승인된 fallback 규칙에 따라 root session의 inline read-only review로 전환했습니다. 따라서 native lane의 독립 verdict나 model/effort provenance는 존재하지 않으며, 이 문서는 inline fallback review의 결과입니다.

## 7-Tier verdict

| Tier | 결과 | 근거 |
|---|---|---|
| T1 정확성·계약 | PASS | 24개 positive row, `TEXT` 21개·`EMPTY` 3개, malformed 3개 제외, manifest 순서·분류·SHA 검증 |
| T2 API·호환성 | PASS | production API·provider·dependency 변경 없음; benchmark source set 내부 계약 |
| T3 동시성·자원 | COMMENT | cold 1회·warmup 2회·warm 3회와 process RSS를 기록하며, warm은 wrapper 재사용일 뿐 fresh Tess4J client 계약을 명시 |
| T4 보안·실패 격리 | PASS | 고정 command만 실행하고 native prerequisite 누락·출력 분류 불일치·RSS 오류를 fail-closed 처리 |
| T5 성능 | PASS | throughput을 latency 역수로 환산하지 않고 250 ms 실제 관측 창에서 산출; 단일 host 관측이라는 한계를 문서화 |
| T6 테스트·검증 | PASS | protocol·contract targeted 7/7 PASS, `validateOcrProtocolReceipt` PASS, 전체 module check PASS |
| T7 유지보수·문서 | PASS | EN/KO README와 benchmark 문서에 실행법·host 한계·receipt 경계를 반영; P2 경계는 회귀 테스트로 고정 |

## Findings

- P0: 0건
- P1: 0건
- P2: 0건 — 초기 review에서 발견한 protocol row `warmIterations`/embedded metric output SHA 교차 검증 누락과 상대 output path 허용을 `e4da40c`에서 수정했습니다.
- P3: 1건 — host-native Tesseract 실행은 기본 hosted CI에서 의도적으로 skip되며, committed receipt validator와 로컬 macOS arm64 실행 결과로 보완합니다. 이는 문서화된 범위 결정이며 merge blocker가 아닙니다.

## 검증 영수증

- `git diff --check`: PASS
- `./gradlew :bluetape4k-images-benchmark:test --tests 'io.bluetape4k.images.benchmark.OcrBenchmarkProtocolTest' --tests 'io.bluetape4k.images.benchmark.OcrBenchmarkContractTest' --console=plain`: 7/7 PASS
- `./gradlew :bluetape4k-images-benchmark:validateOcrProtocolReceipt --console=plain`: PASS, 24 fixtures, CER `0.04604051565377532`, WER `0.09897610921501707`
- 원격 PR #569 Train-1: exact head `a3c70ac22524b06580b72a9f8dfe07707db2e766`, CI `SUCCESS`; host-native test jobs는 scope상 `SKIPPED`

## 결론

inline fallback 기준 `COMMENT`가 최종 verdict입니다. P0/P1 차단 없이 Train-3 PR 생성과 exact-head CI 검증을 진행할 수 있습니다. 이 리뷰는 native independent verdict를 대체하는 bounded fallback이며, native lane timeout 사실을 숨기지 않습니다.
